# 配布アプリのビルド設定（本番）
# このファイルを .env.production にコピーして値を設定し、`npm run build` します。
# vite.config.ts がビルド時にこれらを埋め込みます。

# 接続先バックエンド（デプロイした公開URL）
AIVC_BACKEND_URL=https://api.your-domain.com

# ライセンス検証用の公開鍵（scripts/keygen.py init で生成したもの・サーバと同じ公開鍵）
AIVC_LICENSE_PUBLIC_KEY=
